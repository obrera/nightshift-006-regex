services:
  regex-web:
    image: beeman/static-server:latest
    volumes:
      - ./dist:/workspace/app:ro
    ports:
      - "9010:9876"
    environment:
      - SPA=true
  regex-www:
    image: beeman/static-server:latest
    environment:
      - REDIRECT_URL=https://regex.colmena.dev
